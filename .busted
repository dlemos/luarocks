return {
   default = {
      output = "htest",
      verbose = true,
      ["exclude-pattern"] = "sum_spec", -- do not run spec files inside fixture
      helper = "spec.util.helper",
      ["auto-insulate"] = false
   }
}
